# from Session6F import max_in_list, min_in_list
from Session6F import max_in_list
from Session6F import min_in_list

numbers = [10, 17, 23, 45, 9, 12]
ipl_scores = [111, 210, 120, 97, 56, 32]
product_prices = [100, 50, 77, 80, 12, 99, 20, 200, 76, 55]

max_in_list(numbers)
max_in_list(ipl_scores)
min_in_list(product_prices)